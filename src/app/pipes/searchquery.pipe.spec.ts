import { SearchqueryPipe } from './searchquery.pipe';

describe('SearchqueryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchqueryPipe();
    expect(pipe).toBeTruthy();
  });
});
